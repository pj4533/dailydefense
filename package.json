{
  "name": "daily-defense",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test": "vitest run --coverage",
    "reset-leaderboards": "npx tsx scripts/reset-leaderboards.ts"
  },
  "dependencies": {
    "@upstash/redis": "^1.36.3",
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.0",
    "dotenv": "^17.3.1",
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
